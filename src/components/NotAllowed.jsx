const NotAllowed = () => {
    return (
        <h1>
            Não é possível acessar essa página porque você não está logado ou
            não tem permissão para acessá-la
        </h1>
    );
};

export default NotAllowed;
